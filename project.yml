name: ChatApp
options:
  bundleIdPrefix: com.yourname.chatapp
  deploymentTarget:
    iOS: "16.0"

packages:
  ZIPFoundation:
    url: https://github.com/weichsel/ZIPFoundation.git
    from: "0.9.19"

targets:
  ChatApp:
    type: application
    platform: iOS
    dependencies:
      - package: ZIPFoundation
    sources:
      - Sources/ChatApp
    info:
      path: Sources/ChatApp/Info.plist
      properties:
        UILaunchScreen: {}
        CFBundleDisplayName: ChatApp
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.yourname.chatapp
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1"
        # Không ký khi build trên CI — AltStore/SideStore sẽ tự ký lại khi cài
        CODE_SIGNING_ALLOWED: NO
        CODE_SIGNING_REQUIRED: NO
        CODE_SIGN_IDENTITY: ""
        DEVELOPMENT_TEAM: ""
